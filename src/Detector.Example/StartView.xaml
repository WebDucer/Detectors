<Grid x:Class="Detector.Example.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Detector.Example"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:StartViewModel}">
    <Viewbox Stretch="Uniform">
        <TextBlock FontSize="70" FontWeight="Bold" Text="Example for Detectors" VerticalAlignment="Center" HorizontalAlignment="Center" />
    </Viewbox>

    <Button Content="Start Dianosis" VerticalAlignment="Bottom" Command="{Binding StartCommand}" />
</Grid>
